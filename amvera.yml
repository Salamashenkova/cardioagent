meta:
  environment: python
  toolchain:
    name: pip
    version: "3.12"

build:
  requirementsPath: diploma/requirements.txt

run:
  command: cd diploma && uvicorn api.main:app --host 0.0.0.0 --port 8000
  containerPort: 8000  
  persistenceMount: /data
