meta:
  environment: python
  toolchain:
    name: pip
    version: "3.12"

build:
  requirementsPath: diploma/requirements.txt

run:
  command: cd diploma && streamlit run ui/app.py --server.port 8000 --server.address 0.0.0.0
  containerPort: 8000
  persistenceMount: /data
