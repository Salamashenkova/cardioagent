meta:
  environment: python
  toolchain:
    name: pip
    version: "3.12"
build:
  requirementsPath: requirements.txt
  useCache: true
run:
  scriptName: null
  command: uvicorn diploma.api.main:app
  persistenceMount: /data
  containerPort: "8000"
  servicePort: "80"
